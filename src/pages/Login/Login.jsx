import styles from './Login.module.scss';

const Login = () => {
    return (
        <div className={styles['Login']}>
            {/* Viet HTML o day */}
            <h1>Login page</h1>
        </div>
    );
};

export default Login;
